# Paths excluded from ZIP snapshots created by scripts/snapshot.mjs.
# Same shape as .gitignore (one path/glob per line; # comments OK).
#
# Snapshots are PERSONAL recovery archives — the owner sends them
# to their own Teams self-DM and uses them to restore on a new
# laptop. They are NOT shared with anyone, so credentials are
# allowed (and desired) inside the ZIP. See AGENTS.md
# §"Local credentials" for the policy.

versions/
.git/
.cache/
node_modules/

# OS junk
.DS_Store
Thumbs.db
desktop.ini
