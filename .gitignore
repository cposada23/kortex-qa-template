# ZIP snapshots are local-only artifacts
versions/*.zip
versions/*.tar.gz

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor metadata
.idea/
.vscode/settings.json

# Node (in case future tools add deps)
node_modules/
npm-debug.log
pnpm-debug.log

# Cached state
.cache/

# Secrets — NEVER commit
client-secrets/
*.env
*.env.local
.secrets

# Chat handoff state — ephemeral session context, not knowledge.
# Generated by /chat-handoff. Included in snapshot ZIPs for personal
# laptop-swap recovery (see AGENTS.md §3 + §8).
CHAT-HANDOFF.md

# Import staging — generated by scripts/import-prior-brain.mjs.
# Engineer reviews + applies, then deletes the imports/ folder.
imports/
